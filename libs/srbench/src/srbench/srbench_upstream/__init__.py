# Vendored SRBench modules. See ./NOTICE for provenance (pinned commit) and
# the GPLv3 license that applies to this subpackage. Do not import these
# directly; use ``srbench.driver``, which puts this directory on sys.path so
# their cwd-relative imports resolve.
