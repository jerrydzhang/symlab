Bootstrap: docker
From: python:3.12-slim

%files
    pyproject.container.toml /build/project/pyproject.toml
    libs/symbolic /build/project/libs/symbolic
    models/manifest /build/project/models/manifest
    README.md /build/project/README.md

%post
    apt-get update && apt-get install -y --no-install-recommends git ca-certificates && rm -rf /var/lib/apt/lists/*
    pip install --no-cache-dir uv

    cd /build/project
    export UV_PROJECT_ENVIRONMENT=/opt/venv
    # No --frozen: the container pyproject diverges from the committed lock
    # (git-dep jernerics, cu118 torch, narrowed workspace), so uv re-resolves.
    uv sync --no-dev
    rm -rf /root/.cache /build

%runscript
    exec "$@"

%environment
    export PATH=/opt/venv/bin:$PATH
    export LANG=C.UTF-8
    export LC_ALL=C.UTF-8
